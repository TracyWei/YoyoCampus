function Bianjifenlei(){
	 $('.click-delete').click(function(event) {
       	$('.type-of-shop').show();
        $('.delete-types').show();
    });
    $('.type-of-shop').click(function(event) {
        $('.type-of-shop').hide();
        $('.delete-types').hide();
    });
    $('.cancel-margin').click(function(event) {
        $('.type-of-shop').hide();
        $('.delete-types').hide();
    });
    $('.delete-margin-margin').click(function(event) {
        $('.type-of-shop').hide();
        $('.delete-types').hide();
    });
}